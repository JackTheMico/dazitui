# 意图
中文打字练习核心领域库，负责赛文载入与处理、跟打会话状态机、用户配置以及 52dazi 在线服务交互。

# 内部约束
纯领域与数据处理逻辑，仅依赖网络/加解密库和标准库，不包含任何终端 UI 或 Ratatui 依赖。

# 文件

## Cargo.toml
dazitui-core crate 依赖与 feature 配置

## src/
核心业务模块实现，对外暴露赛文模型、跟打会话、配置持久化及在线客户端 API。
