# dazitui

Linux 上的中文打字练习工具（TUI）。用户对照给定的赛文跟打，实时标红打错的字，打完后得到 WPM 等统计；支持加载本地文件和 52dazi.cn 在线赛文、上传成绩。

## Language

**跟打**:
用户对照给定赛文打字练习的行为。
_Avoid_: 打字测试、敲字

**上屏**:
输入法确认候选后输出到程序的中文字符。TUI 只能收到上屏文本，看不到输入法候选窗口。
_Avoid_: commit（代码里可用）

**赛文**:
练习/比赛用的文字内容，来自本地文件或 52dazi.cn。
_Avoid_: 文章、文本、原文

**回改**:
用户删除已上屏字符重新输入的行为，统计时记为一次错误。
_Avoid_: 退格、删除重打

**载文**:
把赛文加载进跟打区开始练习的动作。
_Avoid_: 加载文章

**错字**:
最终输出与原文逐字比对不一致的字符数，加上期间发生的回改次数。
_Avoid_: 错别字、错误数

**错词**:
包含至少一个错字或回改字符的词汇单位（长度不小于 2 字符）。在内置词组赛文中依据原生词边界判定，在通用连续赛文中依据分词索引判定。
_Contrast_: 错字

**WPM**:
每分钟正确字数（正确字数 / 用时分钟），跟打成绩的主指标。
_Avoid_: words per minute 全称

**击键**:
每秒击键次数（KPS / 击速），跟打成绩的核心指标之一。并击在同一时间按下的多个物理按键统算为一击。
_Contrast_: 按键频率
_Avoid_: 敲击速度、按键数

**串击**:
一次上屏对应一串**先后**按下的键的输入方案，如虎码、五笔、小鹤音形、拼音。代码库中无独立字段，判定为「非并击」（`chord_algebra()` 为空）。实时键盘上这类方案的按键按回放节奏依次点亮，见 ADR 0011。
_Contrast_: 并击
_Avoid_: 顺序击键、连续击键

**目标击键**:
内置单字赛文分组跟打中设定的放行门槛之一，以每秒击键次数（KPS）衡量，以单组为独立考核范围，组内达标才允许放行进下一组。若与目标速度同时设置，两者满足其一即可放行。
_Contrast_: 目标速度

**目标速度**:
内置单字赛文分组跟打中设定的放行门槛之一，以每分钟正确字数（WPM）衡量，以单组为独立考核范围，组内达标才允许放行进下一组。若与目标击键同时设置，两者满足其一即可放行。
_Contrast_: 目标击键

**重打当前组**:
内置单字赛文分组跟打中，当前组敲完但单组指标未达目标击键或目标速度时，系统自动清空本组已上屏输入并重置单组计时，令用户立即重新跟打本组。
_Contrast_: 组边界门槛

**未达标乱序重打**:
内置单字赛文跟打中设定的可配置开关（默认关闭）。开启后，当前组敲满到达组末时，若存在错字、发生过回改或未达到设定的目标速度/击键门槛，系统在清空本组输入与重置计时器的同时，就地随机打乱当前组字符顺序（Shuffle），令用户在新的排列顺序下重新跟打本组；若连续未达标，每次失败均再次重新随机洗牌，直到以零错字、零回改且达标速度一次性通关本组。
_Contrast_: 重打当前组

**码长**:
平均每输出一个已上屏字符所需的总击数（总击数 / 已上屏字数），用于衡量输入方案在当前赛文下的出字效率。
_Contrast_: 击键
_Avoid_: 键长、编码长度

**实时指标**:
跟打进行中在对照区右下角实时渲染更新的动态状态读数，包含实时 WPM 与实时击键。
_Contrast_: 成绩视图

**按键频率**:
统计跟打期间每个键的击键次数，用于分析输入习惯。
_Avoid_: 键盘统计

**完成**:
跟打区上屏文本推进到原文末尾，自动结束并展示成绩；用户也可随时快捷键提前结束（按已打部分统计）。
_Avoid_: 打完、结束

**成绩视图**:
跟打完成后展示成绩与过程分析的视图（含 WPM、错字、用时、WPM 速度折线图与打错时间线明细），按 Esc 返回主界面就绪态，按 Enter/r 重新开始跟打当前赛文（离线赛文），按 Ctrl-Q 退出。
_Avoid_: 成绩面板、结果页、结果界面

**统计视图**:
展示全局历史练习数据分析的独立视图，包含三大子页：WPM 历史演进折线图（支持滚动平均与极值）、按键热力图（支持标准斜列与直列布局切换）、高频错字错词排行榜。
_Avoid_: 历史页、数据面板、分析页面

**有效练习**:
触发完成或主动提前结算进入成绩视图的跟打会话。只有有效练习才会被持久化写入本地统计数据库。
_Contrast_: 中途放弃、空会话

**方案反查**:
根据当前配置的输入法方案，将上屏汉字反向解析为对应物理键位编码序列的过程，用于克服 Linux 终端无法直接获取输入法组字击键的底层限制。
_Avoid_: 码表反编译、按键还原

**编码提示**:
开启"遍码提示"后，在对照区将待输入的输入法编码（拼音/形码字母或并击逻辑码元）渲染在对应字词上方的提示。与方案反查的区别在对象：编码提示显示用户待敲的编码，方案反查解析的是对应物理键位；形码/拼音下二者一致，并击下逻辑码元（如 `wCs`）与物理并击键不同。
_Contrast_: 方案反查
_Avoid_: 反查码、编码预览

**遍码提示**:
设置中的布尔开关（默认关闭），开启后在载文跟打时于对照区显示编码提示。覆盖全部赛文类型（内置/离线/自由发文/剪贴板发文/在线赛文）。
_Contrast_: 编码提示

**键位热力图**:
统计视图中以键盘图形化网格呈现键位按压频次分布的子页面，支持标准斜列与直列布局切换，以及方案投射与原始捕获视角切换。
_Contrast_: 按键频率

**比赛平台**:
提供在线赛文拉取、用户认证与成绩排名的第三方在线跟打竞速服务（目前支持 52dazi.cn 与 虎魄/race.tiger-code.com）。
_Avoid_: 网站、服务器、后端

**虎码杯**:
虎码官方线上打字比赛（平台为 race.tiger-code.com / 虎魄跟打器），每日更新一篇赛文，属于生稿赛，每日每账号仅可载入跟打一次，打完后自动上传成绩并更新当日排行榜。
_Avoid_: 虎杯、虎魄杯

**在线赛文**:
从已连接的比赛平台（如 52dazi.cn 或 虎魄/race.tiger-code.com）载入的比赛用赛文，不可重打，完成后自动上传成绩。
_Contrast_: 离线赛文

**离线赛文**:
从本地文件载入的跟打练习用赛文，可重打，不上传成绩。
_Contrast_: 在线赛文、内置赛文

**内置赛文**:
随程序二进制分发、编译期嵌入的跟打练习用赛文（如常用单字/词组前/中/后五百）。可重打，不上传成绩。无参数启动时默认载入首套内置赛文。渲染时每 10 个一组分页显示：只显示当前组（单字赛文每组 10 字、词组赛文每组 10 个词），当前组十个全对才放行进下一组（组边界门槛）。Session 以 `completed_groups` 跟踪已全对完成的组数，组内可自由打/回改，但回改不可跨越已完成组边界。所有组全部全对才判定会话完成。词组赛文加载时去逗号（用户无需打逗号），渲染时词间加空格显示。
_Contrast_: 离线赛文、在线赛文、自由发文、剪贴板发文

**自由发文**:
用户通过内置多行文本弹窗自由编辑标题与正文（可选保存为本地文件）直接载入跟打的赛文来源。可重打，不上传成绩。
_Contrast_: 离线赛文、内置赛文、剪贴板发文

**剪贴板发文**:
用户一键从系统剪贴板读取当前文本并直接载入跟打的赛文来源。可重打，不上传成绩。
_Contrast_: 离线赛文、内置赛文、自由发文

**功能栏**:
界面左侧的功能与赛文来源菜单，在就绪态或跟打暂停态支持方向键导航选择，开始打字后自动聚焦跟打区。
_Contrast_: 对照区、跟打区

**跟打暂停**:
跟打进行中按 Esc 键主动暂停会话计时；暂停态按 Esc 或 `i` 恢复跟打并解冻计时。
_Tab_ 键在任何主流程态下统一用于收起/展开功能栏，不参与暂停逻辑。
_Contrast_: 就绪态、完成

**上传**:
跟打完成后将成绩（WPM/按键/码长等）自动提交到对应比赛平台。
_Avoid_: 提交、上报

**剪贴板分享**:
上传成功后，将比赛平台返回的排名信息格式化为分享文本，复制到系统剪贴板。自由发文与离线赛文完成时，亦将统计结果格式化为同类分享文本并复制到系统剪贴板。
_Avoid_: 复制成绩

**输入法**:
用户跟打时使用的汉字输入方案（如虎码、五笔、并击等），可在设置中配置，用于成绩上传与剪贴板分享。
_Avoid_: 打字法、输入方案

**指法 Schema**:
定义物理击键到逻辑码元变换规则的 Rime `.schema.yaml` 配置文件（如 `chord_composer.algebra`），用于在实时键盘中将反查码元还原为真实物理按键高亮。
_Contrast_: 方案反查

**主题预设**:
界面的配色方案（如 Catppuccin Mocha、Tokyo Night、Nord 等），控制文字、边框、高亮及状态色彩，支持在设置视图中循环切换与持久化。
_Avoid_: 皮肤、颜色风格

**活动焦点**:
当前接收键盘输入或方向键导航的界面面板（跟打区、功能栏、浏览列表或设置弹窗），通过高亮边框和加粗标题与非活动面板区分。
_Contrast_: 非活动面板

**词提**:
编码提示在单个词组单位上的那份提示（`CodeHint`）。择优于 2026-09-18 定为「确定性做硬门槛、门槛内取击数最少」（ADR 0013）。
_Contrast_: 编码提示
_Avoid_: 提词、词条提示

**确定性**:
打完某个编码后能否用**一次确定按键**拿到目标词：第 1 位按空格；第 2 位按 `;`（仅当方案把 `;` 绑定为选重键）。
第 3 位及以后需数字键或组合键，代价更高且未必可用，不算确定（ADR 0013 D2，2026-09-18 修订）。
_Contrast_: 击键
_Avoid_: 唯一性、无重码

**重码组**:
共享同一个编码的多个词条集合（如万象虎 `jgjr → 个人/佳人/伟人`）。组内按词库权重排序，位次决定该词是否需要选号。
**权重相同时按码表录入顺序**（稳定排序），不得按词条字典序——那会让「的」被「工作」压到第 2 位。
_Contrast_: 确定性

**首选**:
某编码的候选列表里排第一位的词条，由词库权重决定。词提判定确定性时以静态词库权重为准，不反映用户词典调频。
_Contrast_: 重码组

**未收录词**:
词库里查不到整词编码的词（OOV）。定长形码下回退为逐字独立上屏；整句方案下回退为连续码。
_Contrast_: 词提
_Avoid_: 生词、表外词

**简词**:
码表里显式收录整词编码的多字词，码长远短于逐字拼接（虎整句 `慢慢 → hh`、`所以 → m`；
万象虎 `/` 引导的 `一个人 → fjjr/`）。有简词时词提优先给简词。
_Contrast_: 连续码
_Avoid_: 简码（那是单字的短码）

**连续码**:
整句方案下把各个字的码直接连写、中间不按上屏键的输入串（`结果 → igdqem`）。
由语言模型负责切分，击数 = 连续码长 + 一次提交键。
_Contrast_: 简词、逐字独立上屏

**一码段**:
长度为一的码段。整句方案里只在「整段输入只有一码」时合法，
故多字连续码中每字必须取长度 ≥ 2 的码——否则解码会走错切分（`个人` 拼 `jgj` 出「佝」）。
_Contrast_: 连续码

**选重键**:
打完某码后目标词不在第一位时，用来选取它的那个键。虎整句为 `;`（第 2 项）、`'`（第 3 项）、
数字（第 N 项）；万象虎字母表含 `;` 不含 `'`（`'` 是拼音反查前缀），故第 3 项退回数字。
词提直接把该键写在提示串尾部。
_Contrast_: 上屏键
_Avoid_: 选号键（数字只是其中一种）




