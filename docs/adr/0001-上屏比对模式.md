# 跟打采用上屏比对模式

中文输入法在 TUI 里候选窗口不可见（由 X11/Wayland 层绘制），ratatui 只能收到输入法确认上屏后的中文字符。逐键比对对中文不自然（拼音、候选、选字阶段难以和原文对齐），段末一次性比对又没有实时反馈。因此决定：以上屏文本为比对单位，输入法每上屏一段文字就立即与原文对应位置逐字比对、实时标红打错的字，同时记录完整原始按键流（含拼音串和回改）供成绩统计使用。
